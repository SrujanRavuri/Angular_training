<!--
<p>Fetched using ngFor Directive</p>
<ul>
  <li>{{postArray[0]}}</li>
  <li>{{postArray[1]}}</li>
  <li>{{postArray[2]}}</li>
  <li>{{postArray[3]}}</li>
  <li>{{postArray[4]}}</li>
</ul>

<ul>
  <li *ngFor="let post of postArray">{{post}}</li>
</ul>
<p>Fetching obbject array</p>
<button (click)="addNew()">Add New Data</button>
Fetching array index using ngFor


<div *ngIf='objarray.length>0;then postList else noData'>

</div>
<div *ngIf="objarray.length==0">
  <ng-template #postList>
    <ul>
      <li *ngFor="let post of objarray;let i=index">{{i}} {{ post.postTitle}}
      <button (click)="DeletePost(i)">Delete</button></li>
    </ul>
  </ng-template>
<ng-template #noData>
  <p>There is no data to fetch</p>
</ng-template>
//ngSwitch
<div>
  <button (click)="onClick('step 1')">Step1</button>
  <button (click)="onClick('step 2')">Step2</button>
  <button (click)="onClick('step 3')">Step3</button>
</div>
<div [ngSwitch]="stepForm">
<div *ngSwitchCase="'step 1'">Step 1 Form</div>
<div *ngSwitchCase="'step 2'">Step 2 Form</div>
<div *ngSwitchCase="'step 3'">Step 3 Form</div>
<div *ngSwitchDefault>No step clicked</div>
</div>-->
<!--ngStyle
<h1 [ngStyle]="{
  color:isActive?'red':'black',
  textTransform:isActive?'uppercase':'lowercase'
}">NgStyle Directive</h1>-->
<!--ngClass
<h1 [class.main]="isActive" [class.text-weight]="isActive">Hey Angular</h1>
<h1 [ngClass]="{
  'main':isActive,
  'text-weight':isActive
}">Hey again Angular</h1>-->
